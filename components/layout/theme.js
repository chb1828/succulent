import {createTheme} from "@mui/material";

export const customTheme = createTheme({
    palette: {
        common: {
            black: "#000000"
        }
    },
    shape: {
        borderRadius: 16
    }
});